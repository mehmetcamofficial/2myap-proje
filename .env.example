# Public (browser-exposed) configuration — safe to expose, no secrets here.
VITE_PUBLIC_SITE_URL=
VITE_API_BASE_URL=

# Optional business overrides (public)
VITE_BUSINESS_PHONE=
VITE_WHATSAPP_PHONE=

# Server-only (only needed if/when the quote-request API is deployed).
# Never prefix with VITE_. Configure in the Vercel dashboard, not in git.
# DATABASE_URL=
